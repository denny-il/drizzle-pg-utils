{
  "name": "@project/drizzle-utils",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc",
    "check:type": "pnpm run --parallel -r type-check",
    "check:lint": "biome lint .",
    "check:fmt": "biome format .",
    "check": "pnpm run check:fmt && pnpm run check:lint && pnpm run check:type",
    "fmt": "biome check --write .",
    "release": "pnpm check && pnpm build && git add . && "
  },
  "dependencies": {
    "drizzle-orm": "^0.44.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.2",
    "@electric-sql/pglite": "0.3.5",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "files": [
    "dist",
    "tsconfig.json"
  ],
  "version": "0.0.1"
}
